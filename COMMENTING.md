# 注释模板

## 模块注释

```rust
//! 模块名称。
//!
//! 本模块负责：
//! - ...
//! - ...
//!
//! 核心不变量：
//! - ...
//! - ...
//!
//! 修改注意：
//! - ...
```

## 普通函数注释

```rust
/// 函数职责一句话说明。
///
/// 说明该函数在内核中的语义，以及为什么需要这个函数。
///
/// # 参数
///
/// - `arg1`: ...
/// - `arg2`: ...
///
/// # 返回值
///
/// 成功时返回 ...
/// 失败时返回 ...
///
/// # 不变量
///
/// - ...
/// - ...
///
/// # 评测相关
///
/// 影响 ... 测试点。
pub fn example(arg1: usize, arg2: usize) -> isize {
    ...
}
```

## syscall 函数注释

```rust
/// 实现 `xxx` syscall。
///
/// 对应 Linux `xxx` 的部分语义。
/// 当前实现支持 ...
///
/// # 参数
///
/// - `arg1`: ...
/// - `arg2`: ...
///
/// # 返回值
///
/// 成功时返回 ...
/// 失败时返回负 errno，例如：
/// - `-EINVAL`: ...
/// - `-EFAULT`: ...
///
/// # 语义差异
///
/// 与 Linux 完整语义相比，当前实现暂不支持 ...
///
/// # 评测相关
///
/// 影响 busybox / lua / libc-test / lmbench / cyclictest 中的 ...
pub fn sys_xxx(arg1: usize, arg2: usize) -> isize {
    ...
}
```

## unsafe 函数注释

```rust
/// 函数职责一句话说明。
///
/// # Safety
///
/// 调用者必须保证：
/// - ...
/// - ...
/// - ...
///
/// # Errors
///
/// - `EFAULT`: ...
/// - `EINVAL`: ...
pub unsafe fn example_unsafe(ptr: usize) -> Result<(), Errno> {
    ...
}
```

## 结构体注释

```rust
/// 结构体职责一句话说明。
///
/// 表示内核中的 ...
/// 主要参与 ...
pub struct Example {
    /// 字段含义。
    ///
    /// 说明该字段与其他字段的关系、生命周期或并发约束。
    pub field1: usize,

    /// 字段含义。
    pub field2: usize,
}
```

## 行内注释

```rust
// 说明为什么这里需要这样处理，而不是复述代码。
```
